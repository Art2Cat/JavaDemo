<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.art2cat.dev.jpademo.repositories.RoleRepository">
  <insert id="insertRole" parameterType="role">
    insert into role (role_name, create_date, note)
    values(#{roleName}, #{createDate}, #{note})
  </insert>
  <delete id="deleteRole" parameterType="int">
    delete from role where id = #{id}
  </delete>
  <select id="getRole" parameterType="int" resultType="role">
    select id, role_name as roleName, create_date as createDate, note from role
    where id = #{id}
  </select>
  <select id="findRoles" parameterType="string" resultType="role">
    select id, role_name as roleName, create_date as createDate, note from role
    where role_name like concat("%", #{roleName}, "%")
  </select>
  <update id="updateRole" parameterType="role">
    update role
    <set>
      <if test="roleName != null">role_name = #{roleName},</if>
      <if test="note != null">note = #{note}</if>
    </set>
    where id = #{id}
  </update>
</mapper>